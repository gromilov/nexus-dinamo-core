---
interface Props {
  variant?: 'primary' | 'secondary' | 'outline';
  type?: 'button' | 'submit';
  class?: string;
  [key: string]: any;
}

const { variant = 'primary', type = 'button', class: className, href, ...rest } = Astro.props;

const Element = href ? 'a' : 'button';

const variants = {
  primary: "bg-cyber-secondary text-black hover:bg-cyber-primary shadow-[4px_4px_0_rgba(197,160,89,0.3)]",
  secondary: "bg-cyber-primary text-black hover:bg-cyber-secondary shadow-[4px_4px_0_rgba(230,225,214,0.3)]",
  outline: "bg-transparent border-2 border-cyber-secondary text-cyber-secondary hover:bg-cyber-secondary hover:text-black shadow-[2px_2px_0_rgba(197,160,89,0.1)]"
};
---

<Element 
  type={href ? undefined : type} 
  href={href}
  class:list={[
    "py-3 px-6 rounded-none transition-all uppercase tracking-[0.3em] text-[10px] font-black active:scale-[0.98] cursor-pointer relative overflow-hidden group/btn flex items-center justify-center gap-2 no-underline text-center transform border-2 border-black/40",
    variants[variant],
    className
  ]}
  {...rest}
>
  <span class="relative z-10 transition-transform group-hover/btn:scale-105 inline-block">
    <slot />
  </span>
  <div class="absolute top-0 -left-full w-full h-full bg-gradient-to-r from-transparent via-white/10 to-transparent group-hover/btn:animate-[shimmer_1.5s_infinite]"></div>
</Element>

<style>
  @keyframes shimmer {
    100% { left: 100%; }
  }
</style>
